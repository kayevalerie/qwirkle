#include <Helper.h>
#include <string>

// input validation methods

bool fileExists(std::string filename) {
  // todo

  return true;
}

bool isValidTile(char Colour, int Shape) {
  // todo

  return true;
}